export const metadata = {
  title: "Agency Transformation Case Study",
  description:
    "How one engineer transformed a manual-by-default media agency into an automation-first operation — a 3-phase rollout across 205+ files and 8 tools in full production.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
