export const metadata = {
  title: "Budget Tracker Demo",
  description:
    "Interactive simulation of the real-time budget pacing system inside Atlas — live spend monitoring across Google, Meta, and DV360 with automated alerting.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
