export const metadata = {
  title: "AI Print Proofer Demo",
  description:
    "Live simulation of the Gemini Vision print proofing pipeline — autonomously checks creative assets against brand guidelines, replacing a 2-day manual review process.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
