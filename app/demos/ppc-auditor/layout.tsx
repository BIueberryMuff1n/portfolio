export const metadata = {
  title: "PPC Auditor Demo",
  description:
    "Live simulation of the 92-checkpoint PPC audit pipeline — automated campaign quality analysis that replaces hours of manual review across Google and Meta campaigns.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
