export const metadata = {
  title: 'BuildAI Pro — Construction Cost Estimator',
  description: 'AI-powered construction cost estimating for Australian builders. Get instant estimates with material breakdowns, timelines, and market comparisons.',
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
