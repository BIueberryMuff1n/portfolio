export const metadata = {
  title: "Creative Matrix Demo",
  description:
    "Live simulation of a 4-agent AI pipeline: auditor, strategist, taxonomy generator, and vision analyzer — the core orchestration engine inside Atlas Platform.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
